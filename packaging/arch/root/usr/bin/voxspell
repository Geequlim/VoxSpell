#!/bin/sh

if [ -n "${XDG_RUNTIME_DIR:-}" ] && [ ! -S "${XDG_RUNTIME_DIR}/voxspell/daemon.sock" ]; then
	nohup /usr/bin/voxspell-daemon >/dev/null 2>&1 &
fi

exec /usr/lib/voxspell/runtime/bin/node /usr/lib/voxspell/desktop/index.cjs "$@"
